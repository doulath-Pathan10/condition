import { useSelector } from 'react-redux';
import { KeyWithAnyModel, StoreModel, ValidationObjModel } from '../../utils/model/common-model';
import { store } from '../../utils/store/store';
import rulesUtils from './rules.utils';

const Rules_ad_9 = (props: KeyWithAnyModel, s: KeyWithAnyModel): KeyWithAnyModel => {

    const validationObj: ValidationObjModel = {
        nonEditable: [],
        hidden: []
    };
    let hiddenFields;
    
    validationObj.hidden!.push(hiddenFields);

    return rulesUtils(props, validationObj);
}

export default Rules_ad_9;

Please write an test cases using react and jest
